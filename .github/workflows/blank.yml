name: Hello world
    on: workflow_dispatch
    jobs:
        hello:
              runs_on: ubuntu-latest
               steps:
                  -name: hello step
                   run: echo"welcome to gha"
